# Agents.md

## Dev environment tips
- Use uv to manage Python dependencies and virtual environments.
- Use `uv add <package>` to add dependencies.
- Use `uv run <command>` to run commands within the virtual environment.
